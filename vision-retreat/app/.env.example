# Anthropic API キーをここに設定する
# ファイル名を .env.local にリネームしてから値を入れること
# .env.local は .gitignore 対象でコミットされない
VITE_ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxx

# API を一切呼ばず、即ローカル雛形だけ出す（合宿オフライン等）
# VITE_SKIP_ANTHROPIC=true
