# Claude Code まとめ

## Claude Code とは

- Anthropic 提供の**ターミナル上 AI アシスタント**
- **ブラウザ AI との違い**
  - ローカルファイルを直接操作できる
  - `CLAUDE.md` で記憶を蓄積できる
  - Skills 化で再現性を確保できる
- **プログラミング知識不要** — 日本語指示だけで動く

## 4 つの使い方

| 種類 | 概要 |
|------|------|
| Web 版 | ローカル操作は不可 |
| Cowork | 研究段階 |
| IDE 連携（**Cursor**） | **推奨** |
| CLI | 上級者向け |

## Cursor を使う理由

- ファイル構造が見える
- プレビュー可能
- ターミナル統合
- 並列起動に対応

## 料金

- **Cursor Hobby（無料）+ Claude Pro（$20/月）** → 月額おおよそ **3,000 円** で十分なケースが多い
- **Max 5x（$100/月）**、**Max 20x（$200/月）** は上位プラン
- **5 時間ごと・1 週間ごと**に Rate limit あり

## セットアップ手順（STEP 1〜5）

1. **Cursor** をインストール
2. **Anthropic アカウント**を作成し **Pro** を契約
3. **Claude Code** をインストール
4. **ログイン** — 必ず **subscription account** を使う（**API key は従量課金で高額請求のリスク**あり）
5. **Cursor のターミナル**から `claude` を起動

## 重要コマンド 3 つ

| コマンド | 用途 |
|----------|------|
| `/compact` | コンテキスト圧縮（精度が落ちたとき） |
| `/clear` | コンテキスト全クリア（作業を切り替えるとき） |
| `/exit` | 終了 |

## その他の便利コマンド

- `/copy`, `/rewind`, `/fork`, `/model`, `/status-line`
- **Shift+Tab ×2** … プランモード
- **Ctrl+C ×2** … 強制終了

## CLAUDE.md の書き方

- **`/init` は使わない** — 自動生成は精度が下がるという研究結果がある
- **書くべきもの**
  - フォルダの目的
  - 特殊ルール
  - 固有名詞対応表
- **書かなくてよいもの**
  - 一般的なファイル形式の説明
  - 冗長な説明

## コンテキスト管理

- Claude Code はファイル読み込み・コマンド実行もコンテキストに蓄積するため、**消費が早い**
- **基本運用**: 作業 A 完了 → `/clear` → 作業 B 開始
- 長い作業で精度が落ちたら → `/compact` で続行

## プランモード

- **Shift+Tab ×2** で起動 — 計画を立ててから実装に入る
- `/model opus-plan` で **「計画は Opus、実行は Sonnet」** の使い分けが可能
